import ProfileClient from "./ProfileClient";

export const metadata = {
  title: "Profile",
  description: "Update your Pixgen profile settings.",
};

export default function ProfilePage() {
  return <ProfileClient />;
}
